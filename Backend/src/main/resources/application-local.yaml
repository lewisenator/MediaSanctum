spring:
  config:
    activate:
      on-profile: local
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
media-sanctum:
  cookies-secure: false
  log-stack-traces: true