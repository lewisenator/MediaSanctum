<?xml version="1.0"?>
<ruleset name="MediaSanctum PMD Rules"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

  <description>PMD ruleset for MediaSanctum backend.</description>

  <!-- Best practices -->
  <rule ref="category/java/bestpractices.xml">
    <exclude name="GuardLogStatement"/>              <!-- Slf4j/Lombok @Slf4j handles this -->
    <exclude name="JUnitTestContainsTooManyAsserts"/> <!-- Fine in integration tests -->
    <exclude name="JUnitAssertionsShouldIncludeMessage"/> <!-- AssertJ messages are optional -->
    <exclude name="UnusedPrivateMethod"/>             <!-- JPA @PrePersist / @PreUpdate appear unused -->
  </rule>

  <!-- Code style -->
  <rule ref="category/java/codestyle.xml">
    <exclude name="AtLeastOneConstructor"/>           <!-- Lombok @NoArgsConstructor handles this -->
    <exclude name="LongVariable"/>                   <!-- Descriptive names are encouraged -->
    <exclude name="ShortVariable"/>                  <!-- 'id', 'e' etc. are fine -->
    <exclude name="ShortClassName"/>                 <!-- e.g. 'User' is a valid class name -->
    <exclude name="OnlyOneReturn"/>                  <!-- Early returns improve readability -->
    <exclude name="LocalVariableCouldBeFinal"/>      <!-- Too noisy -->
    <exclude name="MethodArgumentCouldBeFinal"/>     <!-- Too noisy -->
    <exclude name="CommentDefaultAccessModifier"/>   <!-- Lombok + Spring don't need this -->
    <exclude name="UseExplicitTypes"/>               <!-- var keyword is allowed in Java 10+ -->
    <exclude name="UnnecessaryAnnotationValueElement"/> <!-- @Annotation("value") style is fine -->
    <exclude name="CallSuperInConstructor"/>         <!-- Spring filter/component constructors don't need super() -->
  </rule>

  <!-- Design -->
  <rule ref="category/java/design.xml">
    <exclude name="LawOfDemeter"/>                   <!-- Very noisy with Spring/Lombok method chains -->
    <exclude name="LoosePackageCoupling"/>
    <exclude name="DataClass"/>                      <!-- DTOs / resource classes are intentional data classes -->
    <exclude name="TooManyMethods"/>                 <!-- Controllers naturally have many endpoints -->
    <exclude name="ExcessiveImports"/>               <!-- Spring Boot services often have many deps -->
    <exclude name="CouplingBetweenObjects"/>
    <exclude name="UseUtilityClass"/>                <!-- Spring Boot main class must have a public constructor -->
    <exclude name="ExceptionAsFlowControl"/>         <!-- False positive without type resolution: PMD can't tell unchecked exceptions aren't caught -->
  </rule>

  <!-- Error prone -->
  <rule ref="category/java/errorprone.xml">
    <exclude name="NullAssignment"/>                 <!-- Sometimes needed for GC hints -->
    <exclude name="AvoidFieldNameMatchingMethodName"/> <!-- Lombok @Data + static factory method pattern -->
    <exclude name="InvalidLogMessageFormat"/>        <!-- PMD false-positive: SLF4J counts Throwable arg separately -->
  </rule>

  <!-- Multithreading -->
  <rule ref="category/java/multithreading.xml">
    <exclude name="UseConcurrentHashMap"/>            <!-- Not always needed -->
    <exclude name="DoNotUseThreads"/>                <!-- Virtual threads / TaskExecutor wrappers are fine -->
  </rule>

  <!-- Performance -->
  <rule ref="category/java/performance.xml"/>

  <!-- Security -->
  <rule ref="category/java/security.xml"/>

</ruleset>
